<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.pixelsfirenotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PixelsFirenotes">
        <activity android:name=".auth.Details"></activity>
        <activity android:name=".auth.Register" />
        <activity android:name=".Splash" />
        <activity android:name=".note.NoteDetails" />
        <activity android:name=".note.EditNote" />
        <activity android:name=".note.AddNote" />
        <activity android:name=".model.Note" />
        <activity
            android:name=".model.Adapter"
            android:label="@string/title_activity_adapter"
            android:theme="@style/Theme.PixelsFirenotes.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>